apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: patient-mongodb-prod
  namespace: argocd
  labels:
    environment: prod
    service: patient-mongodb
spec:
  project: caresync
  source:
    repoURL: https://github.com/caresync-org/argoCD-manifests
    targetRevision: production
    path: charts/mongodb
    helm:
      releaseName: patient-mongodb
      valueFiles:
        - ../../values.yaml
      parameters:
        - name: instanceDatabase
          value: patient
        - name: instancePairedService
          value: patient-service
        - name: global.namespace
          value: caresync-prod
  destination:
    server: https://kubernetes.default.svc
    namespace: caresync-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

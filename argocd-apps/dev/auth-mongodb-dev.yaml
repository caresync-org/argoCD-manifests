apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auth-mongodb-dev
  namespace: argocd
  labels:
    environment: dev
    service: auth-mongodb
spec:
  project: caresync
  source:
    repoURL: https://github.com/caresync-org/argoCD-manifests
    targetRevision: dev
    path: charts/mongodb
    helm:
      releaseName: auth-mongodb
      valueFiles:
        - ../../values.yaml
      parameters:
        - name: instanceDatabase
          value: auth
        - name: instancePairedService
          value: auth-service
        - name: global.namespace
          value: caresync-dev
  destination:
    server: https://kubernetes.default.svc
    namespace: caresync-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

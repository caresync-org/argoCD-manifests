apiVersion: v1
kind: ConfigMap
metadata:
  name: doctor-service-config
  namespace: {{ .Values.global.namespace }}
  labels:
    app: doctor-service
data:
  NODE_ENV: {{ .Values.doctor.env.NODE_ENV | quote }}
  PORT: {{ .Values.doctor.service.port | quote }}
